#!/usr/bin/env bash

if [ "$(uname)" == "Darwin" ]; then #macOS
    :
    # Source macos specific paths
elif [ "$(expr substr $(uname -s) 1 5)" == "Linux" ]; then #Linux
    source "$DOTFILES_DIR/path_linux";
fi
