#!/usr/bin/env bash

if [[ $OS_VARIANT == "mac" ]]; then 
    source "$DOTFILES_DIR/path_macos";
elif [[ $OS_VARIANT == "linux" ]]; then 
    source "$DOTFILES_DIR/path_linux";
elif [[ $OS_VARIANT == "wsl" ]]; then
    source "$DOTFILES_DIR/path_wsl";
fi